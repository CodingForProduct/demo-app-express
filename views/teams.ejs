<!DOCTYPE html>
<html lang="en">
  <head>
  </head>
  <body>
    <h1>Teams</h1>
    <ul>
      <% teams.forEach(function(team){ %>
      <li><%= team.name %></li>
      <% }) %>
    </ul>
  </body>
</html>
