<!DOCTYPE html>
<html lang="en">
  <head>
  </head>
  <body>
    <h1>Users</h1>
    <ul>
      <% users.forEach(function(user){ %>
      <li><%= user.name %></li>
      <% }) %>
    </ul>
  </body>
</html>
